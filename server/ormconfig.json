{
  "type": "postgres",
  "host": "localhost",
  "port": 5000,
  "username": "postgres",
  "password": "postgres",
  "database": "switched",
  "synchronize": true,
  "logging": true,
  "entities": ["../entities/**/*"],
  "migrations": ["src/migrations/**/*"],
  "cli": {
    "entitiesDir": "../entities",
    "migrationsDir": "../migrations"
  }
}
